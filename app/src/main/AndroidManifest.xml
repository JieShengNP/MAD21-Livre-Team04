<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.edu.np.mad.livre">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/livre_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/livre_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Livre">
        <activity android:name=".PopularBookActivity"></activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".RecordActivity" />
        <activity android:name=".StatActivity" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".LibraryActivity" />
        <activity android:name=".ArchiveActivity" />
        <activity
            android:name=".CatalogueActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".BookDetails" />
        <activity
            android:name=".CustomiseBook"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".EditBook" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>